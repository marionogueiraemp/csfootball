/**
 * a companion to the `Pick` type, returns a new object with only the picked attributes
 */
export declare const pick: <T extends Record<string, any>>(obj: T, keys: (keyof T)[]) => Pick<T, keyof T>;
