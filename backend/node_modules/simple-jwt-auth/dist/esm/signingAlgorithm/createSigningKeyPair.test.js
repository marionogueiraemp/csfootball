var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { createSigningKeyPair } from './createSigningKeyPair';
describe('createSigningKeyPair', () => {
    it('should be able to create rsa keypair', () => __awaiter(void 0, void 0, void 0, function* () {
        const keypair = yield createSigningKeyPair('RS256');
        expect(keypair.publicKey.length).toEqual(451);
        expect(keypair.privateKey.length).toBeGreaterThan(1700);
    }));
    it('should be able to create ecc keypair', () => __awaiter(void 0, void 0, void 0, function* () {
        const keypair = yield createSigningKeyPair('ES256');
        expect(keypair.publicKey.length).toEqual(178);
        expect(keypair.privateKey.length).toEqual(241);
    }));
});
//# sourceMappingURL=createSigningKeyPair.test.js.map