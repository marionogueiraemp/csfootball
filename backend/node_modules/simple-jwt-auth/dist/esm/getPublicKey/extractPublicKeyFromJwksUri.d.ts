import { SimpleJwtAuthError } from '../SimpleJwtAuthError';
import { MinimalTokenHeaderClaims } from '../getUnauthedHeaderClaims';
export declare class ExtractPublicKeyFromJwksUriError extends SimpleJwtAuthError {
    constructor({ reason }: {
        reason: string;
    });
}
/**
 * extracts the public key of a token from a jwks uri
 */
export declare const extractPublicKeyFromJwksUri: ({ headerClaims, jwksUri, }: {
    headerClaims: MinimalTokenHeaderClaims;
    jwksUri: string;
}) => Promise<string>;
