export declare const cachePublicKey: ({ issuer, keyId, publicKey, ttlInSeconds, }: {
    issuer: string;
    keyId: string;
    publicKey: string;
    ttlInSeconds?: number | undefined;
}) => void;
export declare const getPublicKeyFromCache: ({ issuer, keyId, }: {
    issuer: string;
    keyId: string;
}) => any;
